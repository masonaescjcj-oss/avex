{
  "name": "@avex/core",
  "version": "0.0.1",
  "private": true,
  "description": "Chain adapters, fee policy, settlement and invoice domain logic",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": "./dist/index.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -b",
    "typecheck": "tsc -b --dry",
    "test": "npm run build && node --test \"dist/**/*.test.js\""
  },
  "dependencies": {
    "@noble/curves": "^2.3.0"
  }
}
